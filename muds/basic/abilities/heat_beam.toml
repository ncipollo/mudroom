name = "Heat Beam"
description = "A focused beam of intense thermal energy."
action_text = "{{character}} fires a heat beam at {{target}}"
engagement_types = ["battle"]
costs = []
role = "attack"
targets = ["opponent"]

[[effects]]
name = "thermal_damage"
trigger_info = { type = "once" }
effect_type = { type = "attribute_update", attribute_id = "hp", value = -15 }
description = { text = "Burns for {{abs_value}} damage" }
